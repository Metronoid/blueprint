models:
  Conference:
    relationships:
      HasMany: Attendee
    columns:
      name: string
      starts_at: datetime
      ends_at: datetime
      venue_id: id
controllers:
  ConferenceController: resource
