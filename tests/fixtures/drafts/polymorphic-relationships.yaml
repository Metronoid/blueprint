models:
  Post:
    relationships:
      morphMany: Image
    columns:
      name: string:400
  User:
    relationships:
      morphMany: Image
    columns:
      name: string:400
  Image:
    relationships:
      morphTo: Imageable
    columns:
      url: string:400
