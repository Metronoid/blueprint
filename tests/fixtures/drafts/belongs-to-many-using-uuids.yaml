models:
  Journey:
    id: uuid
    relationships: string
    columns:
      name: string
      belongsToMany: Diary
  Diary:
    id: uuid
    relationships: string
    columns:
      belongsToMany: Journey
