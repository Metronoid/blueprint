models:
  Certificate:
    relationships:
      belongsTo: \App\Certificate
      hasMany: \App\Certificate
    columns:
      name: string
      reference: string
      document: string
      expiry_date: date
      remarks: text nullable
controllers:
  Api/Certificate:
    resource: api
