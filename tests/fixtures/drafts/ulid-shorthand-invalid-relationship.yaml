models:
  AgeCohort:
    id: string
    timestamps: true
    columns:
      name: string:100
      description: string:500 nullable
      min_age: integer nullable
      max_age: integer nullable
      ulid: ulid unique
